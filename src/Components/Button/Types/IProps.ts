import { ReactNode } from "react"

export type Iprops = {
    backgroundColor?: string
    txtColor?: string
    backgroundHover?: string
    children: ReactNode 
    boxShadow?: string
    primary?: boolean
}